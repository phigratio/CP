#include <bits/stdc++.h>
using namespace std;
#define ll long long
ll ans = 0;
void muqtu()
{
  ll n, m;
  cin >> n;
  vector<int> arr(n + 1);
  for (int i = 0; i < n; i++)
  {
    cin >> arr[i];
  }
  cin >> m;
  vector<int> arr2(m + 1);
  for (int i = 0; i < m; i++)
  {
    cin >> arr2[i];
  }

  ll i = 0, j = 0;
  while (i < n and j < m)
  {
    
  }
}
int main()
{
  ll t;
  cin >> t;
  while (t--)
  {
    muqtu();
  }
}